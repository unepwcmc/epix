<div class="buttons">
  <%= link_to t('cancel_button'), admin_organisations_path, class: 'button btn-scnd' %>
  <input type="submit" value="<%= t('submit_button') %>" form=<%= form_id %> class='button'>
</div>
